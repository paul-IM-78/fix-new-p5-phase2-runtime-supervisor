# NEW-P5-T02-04 Local Balance Observation Report

## Scope

- Worktree: `D:\Ai\staking-wallet-web-p5-t02`
- Branch: `feat/p5-t02-reconciliation`
- Starting HEAD: `b1851b903d2da5c6e6139768e1986eb591587bb8`
- Expected balance baseline: `PASS_EXPECTED_ASSET_BALANCE_BASELINE_COMMITTED`
- Product model: `ASSET_AGGREGATE_RECONCILIATION`

## Observation Schema Contract

Target table:

```text
private.external_balance_observations
```

Columns used:

```text
id uuid
custody_account_binding_id uuid
asset_id uuid
observer_kind text
observation_key text
observed_units numeric
checkpoint_reference text nullable
observed_at timestamptz
created_at timestamptz default clock_timestamp()
```

Binding FK:

```text
private.external_balance_observations.custody_account_binding_id
-> private.custody_account_bindings(id)
```

Asset FK:

```text
private.external_balance_observations.asset_id
-> public.supported_assets(id)
```

Idempotency identity:

```text
custody_account_binding_id + observer_kind + observation_key
```

Existing unique constraint:

```text
external_balance_observations_binding_observer_key_uidx
```

## Provider Observation Contract

Reviewed:

```text
src/server/custody/provider-observation-contract.ts
```

Relevant contract:

```text
CustodyBalanceObservation
provider: CustodyProviderRef
binding: CustodyAccountBindingRef
observedAvailableUnits: string
observedTotalUnits: string
observedAt: string
finalizedAt: string | null
```

The DB ingestion function is not type-coupled to the TypeScript contract. It uses
the existing SQL custody binding, provider, asset, and balance observation
constraints as the authoritative boundary.

## Function

Migration:

```text
supabase/migrations/20260729103000_p5_t02_record_balance_observation.sql
```

Function:

```text
private.record_external_balance_observation(
  p_custody_account_binding_id uuid,
  p_observer_kind text,
  p_observation_key text,
  p_observed_atomic_units numeric,
  p_observed_at timestamptz,
  p_checkpoint_reference text default null
)
```

Return:

```text
external_balance_observation_id uuid
created boolean
```

Volatility and security:

```text
VOLATILE
SECURITY INVOKER
PUBLIC execute revoked
anon execute revoked
authenticated execute revoked
```

No public wrapper RPC was created.

## Asset And Provider Boundary

Asset derivation:

```text
asset_id = private.custody_account_bindings.asset_id
```

The caller cannot provide or override `asset_id`.

Binding/provider observability requires:

```text
private.custody_account_bindings.status = APPROVED
private.custody_providers.status = APPROVED
private.custody_providers.supports_balance_observation = true
public.supported_assets.status = ACTIVE
```

Safe failures:

```text
binding_not_found
binding_not_observable
binding_contract_invalid
```

## Amount Contract

Amount SQL type:

```text
numeric
```

Allowed:

```text
0
positive integer Atomic Units
```

Rejected:

```text
negative values
fractional values
NaN
Infinity
values >= 10^38
```

No JavaScript Number conversion, float, money, rounding, or clamping was added.

## Key And Timestamp Contract

Observation key:

```text
NOT NULL
non-empty
non-whitespace
bounded to the existing 8..200 safe-key shape
secret-like terms blocked by the existing table contract
```

Timestamp:

```text
observed_at is required and preserved exactly
created_at is generated by the database
future timestamps are not rejected by this task
```

## Append-only And Idempotency

First insert:

```text
new row inserted
created = true
```

Identical replay:

```text
existing row selected
same external_balance_observation_id returned
created = false
row count increase = 0
```

Conflicting replay:

```text
safe error = observation_idempotency_conflict
row count increase = 0
existing observed_units unchanged
existing observed_at unchanged
existing checkpoint_reference unchanged
```

The function uses `ON CONFLICT DO NOTHING` followed by payload verification. It
does not use `ON CONFLICT DO UPDATE`, `UPDATE`, or `DELETE` against
`private.external_balance_observations`.

Concurrency boundary:

```text
DB unique constraint = final boundary
advisory lock = not added
application retry = not added
```

## Deferred Side Effects

```text
OBSERVER_CHECKPOINT_UPDATE=DEFERRED
```

The function does not insert or update:

```text
private.observer_checkpoints
private.reconciliation_runs
private.reconciliation_items
private.reconciliation_item_binding_observations
private.external_transaction_observations
private.ledger_journals
private.ledger_entries
```

Only `private.external_balance_observations` may change, and only by append.

## pgTAP

Created:

```text
supabase/tests/database/p5_t02_record_balance_observation.test.sql
```

Coverage:

- Function existence, signature, return shape, volatility, security mode, and comment.
- Browser role execute denial.
- No public balance observation RPC.
- Existing unique idempotency constraint.
- No upsert update, update, or delete function path.
- Missing binding safe failure.
- Approved binding/provider fixture.
- Zero and positive atomic-unit insert.
- Asset derivation from binding.
- Provider classification through binding.
- Exact `observed_at` preservation and DB `created_at`.
- Idempotent replay.
- Conflicting replay for amount, timestamp, and checkpoint.
- Negative and fractional amount rejection.
- Empty and whitespace-only key rejection.
- Non-observable binding rejection.
- Same key independence by different binding and different observer kind.
- Checkpoint, reconciliation, transaction observation, and ledger side effects remain zero.

## Validation Results

Existing DB baseline:

```text
19 files / 1009 tests / failures 0 / skip 0
```

Final DB result:

```text
DB_RESET=PASS
DB_LINT=PASS_ERROR_0_WARNING_0
DB_TEST=PASS_FILES_20_TESTS_1052_FAILURES_0_SKIP_0
GENERATED_TYPE=PASS_DIFF_0
```

Static and security:

```text
NPM_CI=PASS
NPM_AUDIT_PRODUCTION=PASS_VULNERABILITIES_0
NPM_AUDIT_FULL=PASS_VULNERABILITIES_0
LINT=PASS_WARNING_0
BUILD=PASS
```

An initial validation attempt found one implementation error in the new migration:
schema-qualifying `NULLIF` was invalid. The migration was corrected without
changing existing files, and the final authoritative reset, lint, test, type,
audit, lint, and build sequence passed.

## Security Notes

- External provider network calls: 0.
- Remote Supabase calls: 0.
- Service-role runtime: 0.
- Public RPC/API/UI/worker/scheduler: 0.
- Provider SDK or blockchain SDK: 0.
- Raw provider payload storage: 0.
- Credential storage: 0.
- Secret scan: PASS.

## Changed Files

```text
supabase/migrations/20260729103000_p5_t02_record_balance_observation.sql
supabase/tests/database/p5_t02_record_balance_observation.test.sql
docs/09-governance/NEW_P5_T02_04_LOCAL_BALANCE_OBSERVATION_REPORT.md
```

Generated type diff:

```text
src/types/database.types.ts = DIFF_0
```

## Git

```text
Staging=0
Commit=0
Push=0
PR=0
```

## Next Task

Candidate:

```text
P5-T02-05 External observed asset balance aggregation
```

This follows the existing asset aggregate product decision: external balance
observations remain binding-scoped evidence, and a later task should aggregate
them into observed asset-level reconciliation values.

Final status: `PASS_LOCAL_BALANCE_OBSERVATION_READY`.
